export type CardProps = {
  img_front: string;
  img_back: string;
  name: string;
  title: string;
};

export type InfoProps = {
  perfilImg: string;
  description: string;
  name: string;
  age: number;
  birthday: string;
  url?: string;
};